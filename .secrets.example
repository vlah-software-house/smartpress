# SmartPress Environment Configuration
# Copy this file to .secrets and fill in the values.
# NEVER commit .secrets to version control.

# PostgreSQL
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=smartpress
POSTGRES_PASSWORD=changeme
POSTGRES_DB=smartpress

# Valkey (Redis-compatible)
VALKEY_HOST=localhost
VALKEY_PORT=6379
VALKEY_PASSWORD=

# Application
APP_HOST=0.0.0.0
APP_PORT=8080
APP_ENV=development

# AI Provider (choose one: openai, gemini, claude)
AI_PROVIDER=
AI_API_KEY=
AI_MODEL=
